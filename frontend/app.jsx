import React from 'react';
import ReactDOM from 'react-dom';
import Lrl from './lrl'

document.addEventListener('DOMContentLoaded', () => {
    const root = document.getElementById('root');
    ReactDOM.render(<Lrl/>, root);
});